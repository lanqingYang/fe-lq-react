# 1. 父->子：Props

# 2. 子->父：Props + 回调函数

# 3. 兄弟：状态提升，放到父组件(对1，2的组合应用)

# 4. 跨层级/全局： Context + useContext

# 5. 父调用子方法 ：React.createRef/forwardRef

# 6. 事件总线: EventBus mitt

# 7.任意组件通信 : 全局状态（redux、mobx、Zustand）
